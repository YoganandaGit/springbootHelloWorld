syntax = "proto3";
package grpcdemo.proto;
option java_outer_classname = "CommonProto";

// Status of a transaction
message Status {
    bool success = 1; // true if success and false otherwise
    string error = 2; // Message string if not success
    int32 error_code = 3; // Optional error code
}
